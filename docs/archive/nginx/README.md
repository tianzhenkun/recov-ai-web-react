# Nginx 历史配置归档

本目录原样保存此前位于 `deploy/artifacts/` 的 Nginx 配置，归档日期为
2026-07-19。迁移过程中未修改配置内容。

## 文件来源

- `nginx-110.42.223.109/`：原服务器级配置与 Recov Web 站点配置。
- `nginx-81.68.166.109/`：原综合 Web 配置及拆分 API 前的历史版本。

## 使用边界

- 这些文件仅用于历史回溯，不是当前正式部署的配置源。
- 文件包含真实域名、公网 IP、服务器路径、upstream 和证书路径，不能直接复制到新环境。
- 文件中保存的是证书路径，不包含证书或私钥文件；禁止向本目录加入证书、私钥、Token 或密码。
- 当前正式 Web 镜像、Nginx 渲染和发布能力以 `lingchen-release` 仓库为准。
