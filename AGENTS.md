# AGENTS.md

This file provides guidance for AI coding agents when working with code in this repository.

For full project guidance, see [CLAUDE.md](./CLAUDE.md).

## 通用沟通规则

1. 回复需要使用中文。
2. 每次回复时都称呼用户为「[大哥]」。
3. 尊重事实比尊重用户更重要。如果用户判断或前提有误，需要直接指出并说明原因。
4. 从第一性原理分析问题。若需求不明确，需要先提出问题，确认没有疑问后再行动。

## CodeGraph 规则

1. 本项目已初始化 `codegraph`，涉及代码结构理解、符号查询、调用链分析、影响范围判断时，应优先使用 CodeGraph，而不是先进行大范围盲搜或通读文件。
2. 优先使用 `codegraph query`、`codegraph callers`、`codegraph callees`、`codegraph impact`、`codegraph context`、`codegraph files`、`codegraph status` 等能力辅助分析，再按需读取具体文件内容。
3. 在完成任何会影响代码结构或符号关系的修改后，优先执行 `codegraph sync`，保持索引与当前工作区一致。
4. 在开始依赖 CodeGraph 结果进行分析前，如果怀疑索引过期、分支刚切换、或刚执行过 `pull` / 大批量改动，应先执行 `codegraph sync` 再继续分析。
5. `.codegraph/` 仅作为本地索引使用，不纳入业务逻辑、构建流程或版本控制。

## 方案设计偏好

1. 本项目面向商用，方案选择优先考虑稳定性、可维护性、可演进性和业内成熟实践。
2. 不为了兼容旧逻辑牺牲新功能设计，除非用户明确要求兼容旧数据或旧接口。
3. 大型功能或模块重构优先分阶段实施：
   - 先明确表结构和核心状态流转；
   - 再实现核心接口；
   - 最后补充文档和真实外部服务接入。
4. 对外部服务商、复杂基础设施或尚未完全确定的能力，优先使用 mock 或伪实现跑通主流程，待主流程验证后再接入真实能力。
5. 设计文档和接口文档必须包含：
   - 业务规则；
   - 前端交互；
   - 字段含义；
   - 状态流转；
   - 异常场景；
   - 重试策略。
6. 面向前端的文档必须能直接指导页面开发，需要明确：
   - 按钮展示条件；
   - 异步接口含义；
   - 轮询或刷新策略；
   - 字段展示建议。
7. 异步接口返回只表示任务已创建或已受理，不代表业务已完成；必须明确最终状态如何查询。
8. 批量或异步任务不要一次性把全量数据加载进内存，优先按批次分页读取和处理。
9. 失败原因优先统一落到 `error_message` 等明确字段，方便前端展示、用户重试和问题排查。

## 数据库约束

1. 不使用 `jsonb` 等强数据库绑定类型，优先使用通用 `text` / `varchar` 存储 JSON 字符串，保证后续换数据库的兼容性。
2. 不创建物理外键；业务关联通过代码、索引、唯一约束和必要的查询校验保证。
3. 涉及 `bigint` 主键或业务 ID 时，返回前端统一按字符串处理，避免 JavaScript 数字精度丢失。
4. 需要租户隔离的业务表应集成项目已有的 `TenantEntity` 或框架租户能力，不要在各处手写 `tenant_id` 条件。
5. 需要租户隔离的表必须显式增加 `tenant_id` 字段，并让框架统一处理租户过滤。
6. 能用普通索引和业务唯一约束表达的关系，不通过数据库外键强绑定。
7. 表字段命名要贴近业务含义，避免为了临时页面展示增加没有稳定业务价值的冗余字段。
8. 新功能如果明确没有旧数据，不需要为了旧字段或旧状态做兼容设计。

## UI 统一规则

后续迁移页面时，React 页面应在 Ant Design Pro 体系内保持统一的商用后台风格。UI 调整以信息层级清晰、操作路径稳定、表格可读性和可维护性为优先。

1. 统计卡统一使用首页已确定的指标卡风格：多色浅底图标、紧凑数值、单位与主数字拆开显示。
2. 金额类数据使用 `万` / `亿` 等业务化缩写，完整金额通过悬浮提示查看。
3. 列表页标题区只保留页面或模块标题，不放重复统计信息。
4. 记录总数优先放在表格分页的 `showTotal` 中，不在卡片标题右侧重复展示。
5. 不保留无实际信息价值的装饰性标签，例如“快速筛选”“账龄分布”“回款金额”等仅重复说明当前区域含义的文本。
6. 查询区采用统一布局：左侧放筛选条件和“查询 / 重置”，右侧放刷新、导入、导出、批量操作等工具动作；窄屏下允许自动换行。
7. 表格操作列统一固定在右侧。操作项较多时使用 `TableActions` 收纳，避免操作列过宽、换行或遮挡内容。
8. 删除、确认、危险操作使用统一的二次确认样式。文案要说明操作对象、影响范围和不可恢复性，避免只写“确认删除？”这类模糊提示。
9. 页面中不添加说明性质的占位文案来解释功能。可见文本只保留业务字段、操作按钮、状态和必要提示。
10. 图标优先使用 `@ant-design/icons` 或项目已有图标体系，避免临时手写 SVG。
11. 图标颜色应服务于状态和业务分组，不要整页使用单一蓝色。
12. 图表区域保持稳定尺寸和清晰间距。柱状图、折线图、进度条等应优先保证可读性，不为填满空间而拉宽或压缩图形。
13. 弹窗、表单、列表、详情等基础交互应复用项目已形成的布局模式，不为单个页面创造特殊样式，除非业务信息结构确实不同。
